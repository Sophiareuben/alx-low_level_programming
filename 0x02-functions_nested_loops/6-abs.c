#include "main.h"

/**
 * _abs - It computes the absolute value of an integer
 * @n: the int to check
 * Return: the absolute value of int
 * Written by Sophia
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}

